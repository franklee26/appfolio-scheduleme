<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :job_content %>
  </div>

  <div class="field">
    <%= form.label :Tenant_id %>
    <%= form.number_field :Tenant_id, id: :job_Tenant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
