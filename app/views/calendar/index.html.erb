<%= react_component('CalendarIndex', 
    {
        calendars: @calendars, 
        user: @user, 
        user_type: @user.class.name,
        GOOGLE_MAPS_KEY: @GOOGLE_MAPS_KEY
    }) 
%>

<!-- 
        busy_times: @busy_times,
        free_times: @free_times -->
